{% import "macros/img.nunjucks" as img %}

<section id="welcome-section">
    <img class="background normal hidden" />
    <img class="background blurred hidden" />

    {# Navbar partial include #}
    {% include "partials/navbar.nunjucks" %}

    {# Welcome header #}
    <header class="welcome-header">{{welcomeHeader}}</header>
</section>
